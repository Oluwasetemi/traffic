# Base URL for the application
# Used for generating absolute URLs in metadata (OG images, canonical URLs, etc.)
# In development: http://localhost:3000
# In production (Netlify): Set to your site URL (e.g., https://your-site.netlify.app)
# Note: Netlify automatically provides URL, DEPLOY_URL, and DEPLOY_PRIME_URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database Configuration
# Neon Postgres database connection string
# Get your connection string from: https://console.neon.tech
DATABASE_URL=postgresql://user:password@host/database

# Better Auth Configuration
# Secret key for session encryption (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=your_32_char_secret_here
# Base URL for auth callbacks
# In development: http://localhost:3000
# In production (Netlify): Set to your site URL (e.g., https://your-site.netlify.app)
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth Configuration
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Anthropic API Configuration
# Optional: Only needed if you want to use Claude Vision OCR engine
# Get your API key from: https://console.anthropic.com/settings/keys
# Note: Can be set as either ANTHROPIC_API_KEY or NEXT_PUBLIC_ANTHROPIC_API_KEY
# Using ANTHROPIC_API_KEY is more secure as it's server-side only
ANTHROPIC_API_KEY=your_anthropic_api_key_here

NEXT_PUBLIC_VAPID_PUBLIC_KEY=some
VAPID_PRIVATE_KEY=some
