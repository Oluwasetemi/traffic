# Base URL for the application
# Used for generating absolute URLs in metadata (OG images, canonical URLs, etc.)
# In production, this should be your domain (e.g., https://traffic.example.com)
# On Vercel, VERCEL_URL is automatically set and used as fallback
NEXT_PUBLIC_BASE_URL=http://localhost:3000
